import 'package:flutter/material.dart';

/// App color palette matching the dark blue/black design with bright blue accents
class AppColors {
  AppColors._();

  // Background colors
  static const Color backgroundDark = Color(0xFF0A0E27);
  static const Color cardBackground = Color(0xFF151B3D);
  
  // Primary accent colors
  static const Color primaryBlue = Color(0xFF4A90E2);
  static const Color accentBlue = Color(0xFF00B4D8);
  
  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  
  // Additional colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  
  // Status colors (for future use)
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);
}
